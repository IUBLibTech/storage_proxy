<%= form_for(@media_file) do |f| %>
  <% if @media_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@media_file.errors.count, "error") %> prohibited this media_file from being saved:</h2>

      <ul>
      <% @media_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :store_id %><br>
    <%= f.text_field :store_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
