<p id="notice"><%= notice %></p>

<p>
  <strong>Identifier:</strong>
  <%= @work.identifier %>
</p>

<p>
  <strong>Title:</strong>
  <%= @work.title %>
</p>

<%= link_to 'Edit', edit_work_path(@work) %> |
<%= link_to 'Back', works_path %>
